# Fenster — History

## What I Know

- Backend tests should cover all CRUD endpoints
- The API uses in-memory storage, so tests are stateful within a session
- FastAPI provides a TestClient for synchronous testing
- Frontend testing would use Playwright or similar
