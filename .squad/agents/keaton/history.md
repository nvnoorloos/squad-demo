# Keaton — History

## What I Know

- The frontend is an Astro app in `/frontend`
- It fetches tasks from `http://localhost:8000/api/tasks`
- The main page is a Kanban-style task board with three columns: To Do, In Progress, Done
- TaskCard component handles display and status advancement
- Client-side JS handles form submission and task actions via fetch
